export enum TaskType {
  JOIN_TOURNAMENT = 'join tournament',
  INVITE = 'invite',
  VISIT = 'visit',
  SHARE = 'share',
}

export enum TaskCategoryType {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}
