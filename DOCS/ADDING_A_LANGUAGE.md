# Добавление языка — чек-лист по трём репозиториям

Язык живёт не в одном месте. Список ниже собран **проходом по коду**, а не по
памяти, и покрывает всё, что придётся тронуть. Пропуск любого пункта даёт не
падение, а тихий откат на английский — то есть баг, который заметит игрок, а не
сборка.

Пример на испанском (`es`). Порядок деплоя в конце — он важен.

---

## 0. Решить: язык интерфейса или язык рассылок?

Это разные множества, и их регулярно путают.

- **Интерфейс мини-аппы** — то, что игрок выбирает в «Языки». Живой список:
  `en`, `ru`, `de`. Армянский (`hy`) **намеренно выключен**: он есть в enum и в
  базе ради совместимости, но не предлагается как язык приложения.
- **Языки контента** (тексты бота, карточка приглашения, FAQ, задания) — свои
  списки, каждый со своим набором. Ниже помечено, где какой.

---

## 1. Мини-аппа (`lucky-ticket`)

| Файл                                    | Что сделать                                                                                                                                          |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types/enums/locale.enums.ts`       | Добавить в `enum Locale`. Должен совпадать с Prisma-энумом бэка — за этим следит тест `tests/enum-parity.test.ts`.                                   |
| `src/i18n/config.ts`                    | Добавить в `locales` — **это и есть переключатель «язык живой»**. Кука с языком не из списка считается невалидной и откатывается на `defaultLocale`. |
| `messages/<код>.json`                   | Новый файл переводов. Ключи обязаны совпадать с остальными файлами один в один.                                                                      |
| `tests/i18n.test.ts`                    | Добавить код в `LOCALES` — иначе паритет ключей для нового языка никто не проверит.                                                                  |
| `src/hooks/useGetAvailableLanguages.ts` | Пункт в переключателе: код, название, родное название, флаг.                                                                                         |
| `src/constants/flags.constants.ts`      | Флаг уже есть (импортирован весь набор) — просто взять нужный.                                                                                       |

Проверка: `npm test` (паритет ключей + паритет энумов с бэком).

---

## 2. Бэкенд (`lucky-ticket-backend`)

| Файл                                   | Что сделать                                                                                                                                                                                      |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `prisma/schema.prisma`, `enum Locale`  | Добавить значение (в **верхнем** регистре: `ES`). Нужна миграция — enum в Postgres не расширяется сам.                                                                                           |
| `src/common/localized-text.ts`         | `LocalizedText` — обязательные ключи для контента в БД (FAQ, задания, достижения). Добавление ключа **сломает типы во всех каталогах** — это и хорошо, компилятор покажет, где дописать перевод. |
| `src/telegram/invite-share.service.ts` | `SHARE_TEXTS` — встроенная подпись и кнопка карточки приглашения. Это фолбэк на случай пустых полей в админке.                                                                                   |
| `src/telegram/dto/invite-share.dto.ts` | `@IsIn([...])` — иначе мини-аппа получит 400 и уйдёт на запасной путь.                                                                                                                           |
| `src/telegram/start.service.ts`        | `TEXTS` + `pickLang` — приветствие бота по `language_code`.                                                                                                                                      |
| `src/common/economy.constants.ts`      | `INVITE_SHARE_LOCALES` — языки, редактируемые в админке (текст и картинка карточки). Merge-циклы конфига ходят по этому списку, так что там правок не нужно.                                     |
| `src/admin/dto/update-config.dto.ts`   | `InviteShareTextMapDto` и `InviteShareImageMapDto` — поля перечислены **вручную**. Не добавишь — валидатор молча отбросит язык (`whitelist: true`).                                              |
| `src/admin/admin.types.ts`             | `Record<'ru' \| 'en' \| 'de', …>` в `referral.share`, `locale` в `ContractUserDetail`, и `BOT_PROFILE_LOCALES` (языки профиля бота).                                                             |
| `src/admin/dto/user-mutations.dto.ts`  | `@IsIn([...])` для правки языка игрока из админки.                                                                                                                                               |
| `src/profile/profile.service.ts`       | `TICKET_GIFT_MESSAGES` — текст DM при подарке билета.                                                                                                                                            |

Проверка: `npm test`, `PORT=4010 npm run test:e2e`.

---

## 3. Админка (`luckyticket-admin`)

| Файл                                                | Что сделать                                                                                                                                                      |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types/admin.types.ts`                          | `Locale` и `INVITE_SHARE_LOCALES` — из последнего строятся вкладки языков в редакторе карточки.                                                                  |
| `src/constants/global.constants.ts`                 | `localeOrder` и `localeLabels` — **в ВЕРХНЕМ регистре** (`EN`/`RU`), потому что это Prisma-энум. Не перепутать с нижним регистром мини-аппы.                     |
| `src/components/pages/config/InviteShareEditor.tsx` | `LOCALE_LABELS` и `BUILT_IN` — второе дублирует встроенные тексты бэкенда, чтобы показывать их плейсхолдером. Разойдутся — админ увидит не тот текст, что уйдёт. |
| `src/components/pages/users/EditUserModal.tsx`      | `LOCALE_OPTIONS` в форме игрока.                                                                                                                                 |
| `src/mock/data.mock.ts`                             | Мок конфига: `share.texts` и `share.images` — иначе типы не сойдутся.                                                                                            |

---

## 4. Регистр: главная ловушка

Один и тот же язык пишется по-разному в зависимости от слоя:

- **нижний** (`en`, `ru`, `de`) — мини-аппа, кука языка, тело `prepare-share`, контракт админского API;
- **ВЕРХНИЙ** (`EN`, `RU`, `DE`) — Prisma-энум, колонка `User.locale`, фильтры в админке.

Маппинг делают мапперы бэкенда. Перепутанный регистр не падает — он просто не
находит язык и молча отдаёт английский.

---

## 5. Порядок деплоя

1. **Бэкенд** — миграция энума и новые тексты.
2. **Админка** — только после бэка: панель шлёт на сохранение весь конфиг
   целиком, и поле, которого бэкенд ещё не знает, даёт **400 на всё сохранение
   настроек**, а не только на новое поле.
3. **Мини-аппа** — последней: до неё язык всё равно не показывается игроку.

---

## 6. Что НЕ переводится (осознанно)

- **Картинка карточки приглашения** — одна на все языки, если в админке не
  задана отдельная (Настройки → Рефералка → вкладки у картинки).
- **Названия товаров Маркета** — собираются на фронте из тира и типа.
- **Достижения** — тексты в каталоге бэкенда, но выдача-заглушка.
- **Язык получателя приглашения** — недоступен в принципе: карточка собирается
  до выбора чата, а Telegram не сообщает, кому её отправили. Локаль карточки —
  всегда локаль **отправителя**.
